<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <b><%= pluralize(object.errors.count, "error") %> prohibited this <%= object.class.to_s.downcase %> from being saved:</b>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
    </div>
  </div>
<% end %>